# Changelog

## Unreleased

- Documented `@florasync/harmonogram-elements` as the canonical npm launch package for the custom-element surface.
- Documented `<harmonogram-board>` as the supported GA custom element for new production integrations.
- Classified `<harmonogram-wc>` as an unsupported pre-GA starter tag with no backward-compatibility guarantee.
- Added retirement guidance for converting any internal flat `<harmonogram-wc>` usage into `Plan` data rendered by `<harmonogram-board>`.
- Added changelog coverage to the package payload so release notes ship with the npm package.

## 0.1.0

- Initial prelaunch workspace package with board, starter tag, browser bundle, typed ESM entrypoints, and example coverage.
